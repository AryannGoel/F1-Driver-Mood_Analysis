# Pit Wall — The Silent Co-Driver. The app is live-only; all of these are needed.

# --- web API ---
fastapi
uvicorn[standard]
pydantic>=2
python-multipart          # audio-clip uploads (multipart/form-data)

# --- Hugging Face inference (ASR + speech-emotion) ---
transformers
torch                     # CPU build is fine: pip install torch --index-url https://download.pytorch.org/whl/cpu

# --- real F1 lap data + audio ---
fastf1                    # official lap times + absolute lap start times (pulls pandas)
numpy
soundfile                 # decode the radio clips (mp3/wav) — no numba, works on locked-down Windows
soxr                      # resample clips to 16 kHz for the models (numba-free)
librosa                   # NOT used for audio here (numba's DLL is blocked on some machines);
                          # kept only because transformers imports it lazily at pipeline load
requests

# --- optional: local copy of the HF team-radio dataset (python _download_dataset.py) ---
pyarrow                   # read the downloaded parquet shards -> instant dropdowns + imports
